//*a tire

//*tries allows you to know if the letter exist in the body of the text

//*not a binary tree means that it can have the more then 2 children

//*also know as prefix tree, ip routes, autocomplete, suggestions of words

//*O(word of the length);

//*space complexity is also low and isn't very much